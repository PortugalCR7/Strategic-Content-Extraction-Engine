# Doctrines Directory

Place markdown doctrine files (`.md`) in this directory.

Each file represents a strategy doctrine that can be loaded at runtime via `loadDoctrines()` or `loadDoctrine(slug)`.

**Naming convention**: Use kebab-case filenames (e.g. `content-prioritization.md`).
